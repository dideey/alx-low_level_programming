#include "main.h"
/**
 * _isupper - checks for uppercase characters
 * Return: 1 if upper,otherwise return 0 .
 * @c: is a input character
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}

}
